# project_2

Segundo proyecto del stack Python. Sigue la misma estructura que
[credit_scoring](../credit_scoring/README.md): entorno virtual propio en
`.venv/`, código bajo `src/` y salidas (`artifacts/`, `models/`, `mlruns/`,
`reports/`) fuera del control de versiones.
